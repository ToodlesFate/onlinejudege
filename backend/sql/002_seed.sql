-- =============================================================================
--  OnlineJudge — 002_seed.sql
--  SPEC: §4.2 tags 表预置数据
--  用途: 灌入 8 个预置标签 (id 固定 1..8, 不开放后台管理)
--  幂等:  使用 INSERT IGNORE, 重跑不会重复插入
-- =============================================================================

SET NAMES utf8mb4;

INSERT IGNORE INTO `tags` (`id`, `name`, `slug`) VALUES
  (1, '数组',       '数组'),       -- NOTE: SPEC §4.2 原文如此, slug 与 name 同为中文,
                                   --       与其余 7 个 ASCII slug 不一致, 疑似笔误
                                   --       (可能应为 slug='array'); 后续如需修正请同步更新 SPEC
  (2, '字符串',     'string'),
  (3, '链表',       'linked-list'),
  (4, '栈/队列',    'stack-queue'),
  (5, '树',         'tree'),
  (6, '图',         'graph'),
  (7, '动态规划',   'dp'),
  (8, '贪心',       'greedy');
